#pragma once
#include "bayes/PosteriorSimulator.hpp"

namespace bayes {

/** This is a "posterior" simulator that simply generates values from a normal gamma distribution.
 * It is intended to be used mainly as a prior.
 */
class NormalGammaSimulator : public PosteriorSimulator {
    public:
        NormalGammaSimulator(
};

}
